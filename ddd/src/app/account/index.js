const { register } = require('./register');
const { reset } = require('./reset');
const { verify } = require('./verify');

module.exports = {
  register: register,
  reset: reset,
  verify: verify
};
